# TuskPoint API service deps. The engine itself is installed from the repo root
# (see render.yaml / Dockerfile build commands: `pip install ..`).
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
langgraph>=0.2.0
langgraph-checkpoint>=2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
memwal
