# Suggested GitHub repo metadata

Paste these when creating / configuring the `tuskpoint` repo (Settings → top of
the repo page → ⚙ "About").

## Description (one line, < 350 chars)

> Verifiable LangGraph agent checkpoints on Walrus — survive a crash, rewind to
> any moment, and search your history in plain English via MemWal. Ships a
> drop-in checkpointer, a 6-tool MCP server, and a Next.js demo dashboard.

## Shorter alt (if you prefer punchy)

> Drop-in LangGraph checkpointer backed by Walrus + MemWal, with an MCP server
> and a live demo UI. Agent memory that survives a crash.

## Topics (add these tags)

```
walrus
langgraph
mcp
model-context-protocol
checkpointer
ai-agents
agent-memory
decentralized-storage
memwal
sui
nextjs
python
```

## Website field

Set this to your Vercel URL once deployed, e.g.
`https://tuskpoint.vercel.app`

## Social preview (optional)

Settings → "Social preview" → upload a 1280×640 image. A dark screenshot of the
hero section works well.
